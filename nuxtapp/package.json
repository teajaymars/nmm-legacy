{
  "private": true,
  "scripts": {
    "build:css": "sass --no-source-map -I node_modules assets/main.scss assets/compiled.css",
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "yarn build:css && nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "devDependencies": {
    "@types/node": "^20.8.4"
  },
  "dependencies": {
    "@nuxtjs/strapi": "^1.9.3",
    "bulma": "^0.9.4",
    "nuxt": "^3.2.0",
    "sass": "^1.58.0"
  }
}
